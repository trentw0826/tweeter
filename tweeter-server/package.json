{
  "name": "tweeter-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prepare:shared": "npm --workspace tweeter-shared run compile",
    "build": "npm run prepare:shared && npm run compile",
    "generate-demo-data": "npm run build && node dist/scripts/generateDemoData.js",
    "clear-database": "npm run build && node dist/scripts/clearDatabase.js",
    "test": "npm run build && NODE_OPTIONS=--experimental-vm-modules jest --config jest.config.cjs test/unit",
    "test:integration": "npm run build && NODE_OPTIONS=--experimental-vm-modules jest --config jest.config.cjs test/integration",
    "test:all": "npm run test && npm run test:integration",
    "compile": "npm run clean && tsc",
    "clean": "rimraf dist",
    "sam:build": "npm run prepare:shared && sam build --template-file template.yaml",
    "sam:deploy": "npm run sam:build && sam deploy --config-file samconfig.toml --config-env default",
    "sam:deploy:dev": "npm run sam:build && sam deploy --config-file samconfig.toml --config-env dev",
    "sam:deploy:prod": "npm run sam:build && sam deploy --config-file samconfig.toml --config-env prod",
    "sam:local:start-api": "npm run sam:build && sam local start-api --template-file .aws-sam/build/template.yaml --parameter-overrides ApiStageName=dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1017.0",
    "@aws-sdk/client-s3": "^3.1017.0",
    "@aws-sdk/lib-dynamodb": "^3.1017.0",
    "bcryptjs": "^3.0.3",
    "date-fns": "^3.3.1",
    "tweeter-shared": "file:../tweeter-shared",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/bcryptjs": "^2.4.6",
    "esbuild": "^0.25.9",
    "jest": "^29.7.0",
    "rimraf": "^5.0.5"
  }
}
