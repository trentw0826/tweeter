{
  "name": "tweeter-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run compile",
    "precompile": "npm --workspace tweeter-shared run compile",
    "test": "npm run build && node --experimental-specifier-resolution=node --test test",
    "compile": "npm run clean && tsc",
    "clean": "rimraf dist",
    "prepare:layer": "node ./scripts/sync-shared-layer.mjs",
    "sam:build": "npm run build && sam build",
    "sam:deploy": "npm run sam:build && sam deploy"
  },
  "files": [
    "dist"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "date-fns": "^3.3.1",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "tweeter-shared": "file:../tweeter-shared"
  }
}
