services:
  frontend-preview:
    build:
      context: .
      dockerfile: tweeter-web/Dockerfile
      target: runtime
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://127.0.0.1:3000}
    image: tweeter-frontend-preview:latest
    ports:
      - "4173:80"
    restart: unless-stopped
